repos:
  # Backend: ruff lint + format via uv (uses the same ruff version as backend deps)
  - repo: local
    hooks:
      - id: ruff
        name: ruff lint
        entry: bash -c 'cd backend && uv run ruff check --fix "${@/#backend\//}"' --
        language: system
        types_or: [python]
        files: ^backend/
      - id: ruff-format
        name: ruff format
        entry: bash -c 'cd backend && uv run ruff format "${@/#backend\//}"' --
        language: system
        types_or: [python]
        files: ^backend/
      - id: uv-lock-check
        name: uv lock check
        entry: bash -c 'cd backend && uv lock --check'
        language: system
        pass_filenames: false
        files: ^backend/(pyproject\.toml|uv\.lock|packages/harness/pyproject\.toml)$

  # Frontend: eslint + prettier (must run from frontend-hm/ for node_modules resolution)
  - repo: local
    hooks:
      - id: frontend-eslint
        name: eslint (frontend)
        entry: bash -c 'cd frontend-hm && npx eslint --fix "${@/#frontend-hm\//}"' --
        language: system
        types_or: [javascript, tsx, ts]
        files: ^frontend-hm/

      - id: frontend-prettier
        name: prettier (frontend)
        entry: bash -c 'cd frontend-hm && npx prettier --write "${@/#frontend-hm\//}"' --
        language: system
        files: ^frontend-hm/
        types_or: [javascript, tsx, ts, json, css]
