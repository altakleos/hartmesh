[project]
name = "deerflow-runtime-api"
version = "0.1.0"
description = "Host-independent durable invocation Protocol and immutable contracts"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["deerflow_runtime_api"]
