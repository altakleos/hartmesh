{{- if .Values.provisioner.enabled -}}
{{- include "deer-flow.validateSandboxVolumeMode" . -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "deer-flow.fullname" . }}-provisioner
  namespace: {{ include "deer-flow.namespace" . }}
  labels:
    {{- include "deer-flow.labels" . | nindent 4 }}
    app.kubernetes.io/component: provisioner
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "deer-flow.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: provisioner
  template:
    metadata:
      labels:
        {{- include "deer-flow.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: provisioner
    spec:
      {{- include "deer-flow.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "deer-flow.provisionerServiceAccountName" . }}
      securityContext:
        # Non-root: provisioner image has no USER directive. uid/gid 1000.
        # No fsGroup — it mounts no PVCs (it references PVC names for the
        # sandbox Pods it spawns, never mounting them itself).
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: provisioner
          image: {{ include "deer-flow.provisionerImage" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          ports:
            - name: http
              containerPort: 8002
          env:
            # Running as non-root uid 1000: suppress .pyc writes under root-owned /app.
            - name: PYTHONDONTWRITEBYTECODE
              value: "1"
            # Sandbox resources can live in a separate pre-created namespace,
            # while TokenReview must keep validating the Gateway's release identity.
            - name: K8S_NAMESPACE
              value: {{ include "deer-flow.sandboxNamespace" . | quote }}
            - name: PROVISIONER_AUTH_AUDIENCE
              value: {{ .Values.provisioner.gatewayTokenAudience | quote }}
            - name: PROVISIONER_GATEWAY_NAMESPACE
              value: {{ include "deer-flow.namespace" . | quote }}
            - name: PROVISIONER_GATEWAY_SERVICE_ACCOUNT
              value: {{ include "deer-flow.serviceAccountName" . | quote }}
            - name: SANDBOX_IMAGE
              value: {{ .Values.provisioner.sandboxImage | quote }}
            - name: SANDBOX_RUNTIME_CLASS
              value: {{ .Values.sandbox.runtimeClassName | default "" | quote }}
            {{- if .Values.sandbox.volumeMode }}
            - name: SANDBOX_VOLUME_MODE
              value: {{ .Values.sandbox.volumeMode | quote }}
            {{- end }}
            # In-cluster API access via the mounted ServiceAccount token.
            # K8S_API_SERVER is intentionally unset (do NOT use host.docker.internal).
            # Service type for per-sandbox Services. ClusterIP (default) routes the
            # gateway->sandbox hop over cluster DNS; NodePort is the compose/hybrid
            # escape hatch (gateway not in K8s), which needs NODE_HOST below.
            - name: SANDBOX_SERVICE_TYPE
              value: {{ .Values.provisioner.sandboxServiceType | default "ClusterIP" | quote }}
            {{- if eq (default "ClusterIP" .Values.provisioner.sandboxServiceType) "NodePort" }}
            # NODE_HOST: address the gateway uses to reach sandbox NodePorts
            # (NodePort mode only). When `provisioner.nodeHost` is set, use it;
            # otherwise default to this pod's node IP via the downward API. A
            # NodePort is exposed on every node, so <node-IP>:<NodePort> routes
            # from the gateway on most clusters. Override only when pod->node-IP
            # traffic is blocked by the CNI/policy.
            - name: NODE_HOST
              {{- if .Values.provisioner.nodeHost }}
              value: {{ .Values.provisioner.nodeHost | quote }}
              {{- else }}
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
              {{- end }}
            {{- end }}
            # PVC mode — sandbox Pods mount the same PVCs the gateway uses.
            {{- if .Values.persistence.home.enabled }}
            - name: USERDATA_PVC_NAME
              value: {{ include "deer-flow.homePVC" . | quote }}
            {{- end }}
            {{- if .Values.skills.existingClaim }}
            - name: SKILLS_PVC_NAME
              value: {{ .Values.skills.existingClaim | quote }}
            {{- end }}
            - name: SANDBOX_CONTAINER_PORT
              value: {{ .Values.provisioner.sandboxPort | quote }}
            - name: ACCEPTED_SKILL_PROJECTION_PROFILE
              value: {{ .Values.provisioner.acceptedSkillProjectionProfile | default "disabled" | quote }}
            {{- if eq (default "disabled" .Values.provisioner.acceptedSkillProjectionProfile) "rwx_verified_copy_v2" }}
            # The digest-pinned provisioner image contains both the verifier and
            # the per-attempt data-plane gate.
            - name: ACCEPTED_SKILL_RUNTIME_IMAGE
              value: {{ include "deer-flow.provisionerImage" . | quote }}
            - name: ACCEPTED_ATTEMPT_LEASE_SECONDS
              value: {{ .Values.provisioner.acceptedAttempt.leaseSeconds | quote }}
            - name: ACCEPTED_ATTEMPT_RECONCILE_INTERVAL_SECONDS
              value: {{ .Values.provisioner.acceptedAttempt.reconcileIntervalSeconds | quote }}
            - name: ACCEPTED_ATTEMPT_RECONCILE_LIMIT
              value: {{ .Values.provisioner.acceptedAttempt.reconcileLimit | quote }}
            {{- end }}
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          {{- with .Values.provisioner.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
{{- end -}}
